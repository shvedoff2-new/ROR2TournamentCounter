<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <FontFamily x:Key="Bombardier" >/Resources/Fonts/#Bombardier</FontFamily>
    <Style TargetType="TextBlock" x:Key="TBBlue">
        <Setter Property="FontFamily" Value="{DynamicResource Bombardier}"/>
        <Setter Property="Foreground" Value="#c2d6ef"/>
    </Style>
    <Style TargetType="TextBox" x:Key="TBNick">
        <Setter Property="FontFamily" Value="{DynamicResource Bombardier}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="#302a36"/>
        <Setter Property="BorderBrush" Value="#575262"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="FontSize" Value="75"/>
        <Setter Property="Width" Value="325"/>
        <Setter Property="Height" Value="80"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>
    <Style TargetType="Button" x:Key="Btn">
        <Setter Property="FontFamily" Value="{DynamicResource Bombardier}"/>
        <Setter Property="Background" Value="#435466"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#8a99af"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="0">
                        <ContentPresenter x:Name="contentPresenter"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Content="{TemplateBinding Content}"
                                      ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                      Focusable="False"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#b7be98"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#d8e3bd"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#3a4755"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#6b7a8f"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="ComboBoxItem" x:Key="ComboBoxItemStyle">
        <Setter Property="FontFamily" Value="{DynamicResource Bombardier}"/>
        <Setter Property="Background" Value="#435466"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#8a99af"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8,4"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Padding="{TemplateBinding Padding}">
                        <ContentPresenter x:Name="contentPresenter"
                                      HorizontalAlignment="Left"
                                      VerticalAlignment="Center"
                                      Content="{TemplateBinding Content}"
                                      ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                      ContentTemplate="{TemplateBinding ContentTemplate}"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#b7be98"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#d8e3bd"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="ComboBox" x:Key="ComboBoxStyle">
        <Setter Property="FontFamily" Value="{DynamicResource Bombardier}"/>
        <Setter Property="Background" Value="#435466"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#8a99af"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource ComboBoxItemStyle}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="toggleButton"
                                  Grid.Column="2"
                                  Background="{TemplateBinding Background}"
                                  BorderBrush="{TemplateBinding BorderBrush}"
                                  BorderThickness="{TemplateBinding BorderThickness}"
                                  IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                  ClickMode="Press">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="20"/>
                                            </Grid.ColumnDefinitions>

                                            <ContentPresenter Grid.Column="0"
                                                          x:Name="contentPresenter"
                                                          Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                          ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                          ContentStringFormat="{Binding SelectionBoxItemStringFormat, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                          HorizontalAlignment="Left"
                                                          VerticalAlignment="Center"
                                                          TextElement.Foreground="White"/>

                                            <Path Grid.Column="1"
                                              x:Name="arrow"
                                              Data="M 0 0 L 4 4 L 8 0 Z"
                                              Fill="White"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#b7be98"/>
                                            <Setter TargetName="border" Property="BorderBrush" Value="#d8e3bd"/>
                                            <Setter TargetName="arrow" Property="Fill" Value="#2c3e50"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#3a4755"/>
                                            <Setter TargetName="border" Property="BorderBrush" Value="#6b7a8f"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <Popup x:Name="popup"
                           IsOpen="{TemplateBinding IsDropDownOpen}"
                           Placement="Bottom"
                           PopupAnimation="Slide">
                            <Border Background="#435466"
                                BorderBrush="#8a99af"
                                BorderThickness="1"
                                MinWidth="{TemplateBinding ActualWidth}"
                                MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="SurvivorRadioButtonStyle" TargetType="RadioButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="MainBorder"
                            Width="80" Height="80"
                            Background="#2d2728"
                            BorderBrush="#888282"
                            BorderThickness="2"
                            Cursor="Hand">
                        <Grid>
                            <ContentPresenter x:Name="SurvivorContent"
                                            Content="{TemplateBinding Content}"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                            <Border x:Name="SelectionOverlay"
                                    Background="#fbfec0"
                                    Opacity="0"
                                    CornerRadius="2"/>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="#fbfec0"/>
                            <Setter TargetName="MainBorder" Property="BorderThickness" Value="3"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="#aaaaaa"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsChecked" Value="True"/>
                            </MultiTrigger.Conditions>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="SurvivorRadioButtonCompactStyle" TargetType="RadioButton" BasedOn="{StaticResource SurvivorRadioButtonStyle}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="MainBorder"
                            Width="40" Height="40"
                            Background="#2d2728"
                            BorderBrush="#888282"
                            BorderThickness="2"
                            Cursor="Hand">
                        <Grid>
                            <ContentPresenter x:Name="SurvivorContent"
                                            Content="{TemplateBinding Content}"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>

                            <Border x:Name="SelectionOverlay"
                                    Background="#fbfec0"
                                    Opacity="0"
                                    CornerRadius="1"/>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="#fbfec0"/>
                            <Setter TargetName="MainBorder" Property="BorderThickness" Value="3"/>
                        </Trigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="#aaaaaa"/>
                        </Trigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsChecked" Value="True"/>
                            </MultiTrigger.Conditions>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="CheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Border x:Name="MainBorder"
                            Width="200" Height="20"
                            Background="#435466"
                            BorderBrush="#8a99af"
                            BorderThickness="2"
                            Cursor="Hand">
                        <Grid>
                            <ContentPresenter x:Name="contentPresenter" Margin="5,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Content="{TemplateBinding Content}"
                                ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                Focusable="False"/>
                            <Border x:Name="CheckBorder" Width="15" Height="15"
                                    Background="#435466"
                                    BorderBrush="#8a99af"
                                    BorderThickness="3" Margin="0,0,10,0"
                                    HorizontalAlignment="Right" VerticalAlignment="Center" Cursor="">
                            </Border>
                            <Canvas Width="20" Height="15" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,7,0">
                                <Polygon x:Name="Check" Points="0,9.5 7,15 20,2 17.5,0 6.5,11.5 2,7.5" Fill="#00000000"/>
                            </Canvas>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MainBorder" Property="Background" Value="#b7be98"/>
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="#d8e3bd"/>
                            <Setter TargetName="CheckBorder" Property="Background" Value="#b7be98"/>
                            <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#d8e3bd"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="MainBorder" Property="Background" Value="#3a4755"/>
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="#6b7a8f"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Check" Property="Fill" Value="#cfdfed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>